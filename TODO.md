# pgm-studio — TODO (current focus)

The **Now & Next** board — only the *current focus theme* lives here. Everything not in the immediate
slice is in **`BACKLOG.md`** (the long tail); shipped capabilities are in **`FEATURES.md`** (the Done
column). The three move left → right: **`BACKLOG.md` → `TODO.md` → `FEATURES.md`**.

**Holds only open work:** `[ ]` to-do, `[~]` in progress — **never `[x]`.** When a task ships, a commit
lands (its message references the id), the task **leaves this file**, and a line is added to `FEATURES.md`.
Board rules live in `CLAUDE.md` (§ "Status & task board").

**Three numbers are the author's and are not to be re-derived.** A protection region is at most **20×30**
blocks (`ST10`), a building footprint at most **20×20** (`ST9`), and the smallest room with no building over
it is **4×4** (`WX2`). A dressed prop's 192-cell ceiling (`HP3`) and a room building's 20×20 measure the same
concept since `WE71`, and holding them apart is a deliberate not-yet.

**The mapgen-authoring programme is empty, and this board is between focuses (author, 2026-09-07).** Read
against the code, four of its five were withdrawn rather than worked — `B262` and `B265` asked for surfaces
over reads nobody takes that way, `B150` for an evaluator tier the context cannot carry, and `B171` for a
vocabulary that does not describe a hand-drawn board — and the fifth, `TN2`, shipped (`FEATURES.md`).
`docs/backlog-strategy.md` § *Withdrawal is a legitimate answer* carries what each withdrawal cost.

**Nothing is pulled up yet.** The next group comes from `BACKLOG.md` whole, as a concept rather than a task at
a time. Anything found while working goes there.
